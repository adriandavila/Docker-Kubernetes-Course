This module's project had me use docker-compose to connect multiple containers and also included automatically restarting containers, either always, on-faliure, never (no) or unless we specify it to.
